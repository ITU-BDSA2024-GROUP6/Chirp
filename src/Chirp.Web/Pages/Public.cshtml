@page "/"
@using Chirp.Core.Models
@inject SignInManager<Author> SignInManager
@model Chirp.Web.Pages.PublicModel
@{
    ViewData["Title"] = "Chirp!";
    Layout = "Shared/_Layout";
}

<div>
    <h2> Public Timeline</h2>
    @if (SignInManager.IsSignedIn(User))
    {
        <div class="cheepbox">
            <h3>What's on your mind @(User.Identity.Name)?</h3>
            <form method="post">
                <input style="float: left" type="text" asp-for="Text" id="cheepInput" maxlength="160" oninput="updateCharCount()" />
                <span id="charCount" style="margin-left: 15px;" >0</span>/160
                <input type="submit" value="Share" />
            </form>
        </div>
    }
    @if (Model.Cheeps.Any())
    {
        <ul id="messagelist" class="cheeps">
            @foreach (var cheep in Model.Cheeps)
            {
                <li class="cheep">
                    <p>
                        <strong>
                            <a href="/@cheep.Author.Name">@cheep.Author.Name</a>
                        </strong>
                        @cheep.Text
                        <small>&mdash; @cheep.TimeStamp</small>
                    </p>
                </li>
            }
        </ul>
        <div class="pagination">
            @if (Model.CurrentPage > 0)
            {
                <a href="?page=@(Model.CurrentPage - 1)">Previous</a>
            }

            @if (Model.Cheeps.Count == 32) // Assuming 32 is the page size
            {
                <a href="?page=@(Model.CurrentPage + 1)">Next</a>
            }
        </div>
    }
    else
    {
        <em>There are no cheeps so far.</em>
    }
</div>
@section Scripts {
    <script>
        function updateCharCount() {
            var input = document.getElementById('cheepInput');
            var charCount = input.value.length;
            document.getElementById('charCount').textContent = charCount;
        }
    </script>
}